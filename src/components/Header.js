import React from "react";

function Header({props}) {
    return(
        <main>
            <header>
                <h1>Und</h1>
            </header>
            </main>
    )
}

export default Header;